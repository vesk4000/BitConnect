[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lab1-pow-ipv8"
version = "0.1.0"
description = "CS4160 Lab 1 PoW over IPv8 client"
requires-python = ">=3.10"
dependencies = [
  "pyipv8 @ git+https://github.com/Tribler/py-ipv8.git",
  "pytest>=8.0.0",
  "black>=24.0.0",
  "pre-commit>=3.7.0",
]

[project.scripts]
lab1 = "lab1_pow_ipv8.main:main"
lab2-prep = "lab2_relay_race.prep_cli:main"
lab2-race = "lab2_relay_race.race_cli:main"
lab3-node = "lab3_blockchain.cli:main"
lab3-run-local = "lab3_blockchain.run_lab3_nodes:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
lab3_blockchain = ["bootstrap_servers.json"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.black]
line-length = 88
target-version = ["py310"]
include = "\\.pyi?$"
